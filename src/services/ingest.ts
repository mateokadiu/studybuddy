/**
 * Document ingest worker.
 *
 * Stub for commit 31; wired end-to-end in commit 32.
 */

export async function ingestPdf(docId: string, _uri: string): Promise<void> {
  void docId;
}

export async function ingestPastedText(docId: string, _text: string): Promise<void> {
  void docId;
}
